# Scripts

Future Pyrite64 bindings should live here.

Expected first scripts:

- player object script
- collectible object script
- global game-state script

The gameplay code in `../gameplay/` is intentionally separate from engine-facing scripts so the movement rules stay easy to test and tune.

